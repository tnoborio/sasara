services:
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: sasara
      POSTGRES_USER: sasara
      POSTGRES_PASSWORD: sasara
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
